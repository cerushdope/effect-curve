{
  "name": "effect-curve",
  "version": "1.0.0",
  "description": "Log what you took, how much, and when — see a clean graph of **felt effect over time** (onset → rise → peak/plateau → offset → baseline) for each substance on a shared timeline.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.49.1"
  }
}
